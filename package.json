{
  "name": "haustier-technik-ratgeber",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.js",
    "build": "node scripts/build-categories.js && node scripts/build-public.js",
    "dev": "node scripts/serve.js"
  },
  "engines": {
    "node": ">=20"
  }
}
